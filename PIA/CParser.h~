#pragma once

#include "c.tab.hh"

namespace C {
  class Parser {
   public:
    Parser() : parser(scanner) {}
		
    int parse() {
      return parser.parse();
    }

    void error(const std::string &msg);
		
    private:
      C::FlexScanner scanner;
      C::BisonParser parser;
  };
}
