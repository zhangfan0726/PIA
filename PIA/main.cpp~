#include "PIAScanner.h"
#include "PIAParser.h"
//#include "semantic_tree.h"

// Entry Point
int main(int argc, char * argv[]) {
  PIA::Parser parser;

    //  semantic_tree_node *p=new semantic_tree_intconst_node;
  return parser.parse();
}

